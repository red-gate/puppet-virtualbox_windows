if((& '<%= scope['::virtualbox_windows::vboxmanage'] %>' list -l hostonlyifs) -like 'Name:*<%= @adapter_name %>')
{
  exit 1
}
else
{
  # exit 0 (e.g. adapter will be created) only if the "vboxmanage list hostonlyifs" did not report an error
  exit $LASTEXITCODE
}
